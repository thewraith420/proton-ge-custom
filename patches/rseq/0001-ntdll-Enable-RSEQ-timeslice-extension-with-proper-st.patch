From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Bob <bob@localhost>
Subject: [PATCH] ntdll: Fix RSEQ slice_ctrl access to match test program

Use proper struct access like test_grant.c does, and add debug output
to verify the RSEQ structure is being found and accessed correctly.
---
 dlls/ntdll/unix/loader.c |  9 ++++++++-
 dlls/ntdll/unix/thread.c | 38 +++++++++++++++++++++++++++++++++-----
 2 files changed, 41 insertions(+), 6 deletions(-)

diff --git a/dlls/ntdll/unix/loader.c b/dlls/ntdll/unix/loader.c
index 1111111111..2222222222 100644
--- a/dlls/ntdll/unix/loader.c
+++ b/dlls/ntdll/unix/loader.c
@@ -59,6 +59,7 @@
 #endif
 #ifdef __linux__
 # include <sys/prctl.h>
+# include <errno.h>
 #endif
 #ifdef __APPLE__
 # include <CoreFoundation/CoreFoundation.h>
@@ -2404,7 +2405,13 @@ static void init_rseq_slice_extension(void)
 {
 #ifdef __linux__
     /* Enable RSEQ timeslice extension for this process (BobZKernel feature).
      * Silently ignored on kernels without support. */
-    prctl(PR_RSEQ_SLICE_EXTENSION, PR_RSEQ_SLICE_EXTENSION_SET, PR_RSEQ_SLICE_EXT_ENABLE, 0, 0);
+    int ret = prctl(PR_RSEQ_SLICE_EXTENSION, PR_RSEQ_SLICE_EXTENSION_SET, PR_RSEQ_SLICE_EXT_ENABLE, 0, 0);
+    if (ret == 0)
+        ERR("RSEQ: Timeslice extension ENABLED for process\n");
+    else
+        ERR("RSEQ: Timeslice extension not available (ret=%d, errno=%d)\n", ret, errno);
 #endif
 }

diff --git a/dlls/ntdll/unix/thread.c b/dlls/ntdll/unix/thread.c
index 1111111111..2222222222 100644
--- a/dlls/ntdll/unix/thread.c
+++ b/dlls/ntdll/unix/thread.c
@@ -1139,6 +1139,21 @@ static int get_alt_stack_zero_bits( void )
 }


+/* RSEQ structure matching kernel/glibc definition */
+struct rseq_slice_ctrl {
+    union {
+        uint32_t all;
+        struct {
+            uint8_t request;
+            uint8_t granted;
+            uint16_t __reserved;
+        };
+    };
+};
+
+struct rseq_abi {
+    uint32_t cpu_id_start;
+    uint32_t cpu_id;
+    uint64_t rseq_cs;
+    uint32_t flags;
+    uint32_t node_id;
+    uint32_t mm_cid;
+    struct rseq_slice_ctrl slice_ctrl;
+} __attribute__((aligned(32)));
+
 /***********************************************************************
  *           init_thread_rseq_slice
  *
@@ -1148,20 +1163,33 @@ static int get_alt_stack_zero_bits( void )
  */
 #ifdef __linux__
 extern ptrdiff_t __rseq_offset __attribute__((weak));
 extern unsigned int __rseq_size __attribute__((weak));

 static void init_thread_rseq_slice(void)
 {
-    if (&__rseq_offset != NULL && __rseq_size >= 32) {
+    struct rseq_abi *rseq;
         char *tp;
+
 #if defined(__x86_64__)
         __asm__ volatile("mov %%fs:0, %0" : "=r"(tp));
 #elif defined(__aarch64__)
         __asm__ volatile("mrs %0, tpidr_el0" : "=r"(tp));
 #else
+        ERR("RSEQ: Architecture not supported\n");
         return;
 #endif
-        /* Set request bit at offset 28 (slice_ctrl.request) */
-        *(volatile unsigned char *)(tp + __rseq_offset + 28) = 1;
+
+    /* Check if glibc has registered RSEQ */
+    if (__rseq_offset == 0) {
+        static int warned = 0;
+        if (!warned++) ERR("RSEQ: __rseq_offset is 0, glibc RSEQ not registered\n");
+        return;
     }
+
+    rseq = (struct rseq_abi *)(tp + __rseq_offset);
+
+    /* Set request bit like test_grant.c does */
+    rseq->slice_ctrl.request = 1;
+    __asm__ __volatile__("" ::: "memory");
+
+    ERR("RSEQ: Set request bit for thread (rseq=%p, cpu_id=%u, offset=%ld)\n",
+        rseq, rseq->cpu_id, (long)__rseq_offset);
 }
 #endif
--
2.39.2

